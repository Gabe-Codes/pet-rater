<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="icon" href="/images/pet-rater-logo.png">
		<link rel="stylesheet" href="/stylesheets/style.css" />
    <script defer src="/javascripts/script.js"></script>
		<% if (title === 'Pet Rater Home') { %>
    <link rel="stylesheet" href="/stylesheets/home/home.css" />
    <script defer src="/javascripts/home/home.js"></script>
		<% } %> <% if (title === 'Pet Rater') { %>
		<link rel="stylesheet" href="/stylesheets/main/main.css" />
		<% } %> <% if (title === 'New Pet') { %>
		<link rel="stylesheet" href="/stylesheets/new/new.css" />
		<% } %> <% if (title === 'Pet Profile') { %>
    <link rel="stylesheet" href="/stylesheets/show/show.css" />
		<% } %>
		<title><%= title %></title>
	</head>
	<body>
		<nav>
			<section>
				<img id="logo" src="/images/pet-rater-logo.png" />
			</section>
			<div>
				<a class="title" href="/main">Pet Rater</a>
			</div>
			<% if (user) { %>
			<a class="log" href="/logout">Log Out</a>
			<% } else { %>
			<a class="log" id="login" href="/auth/google">Login</a>
			<% } %>
		</nav>
		<main>
